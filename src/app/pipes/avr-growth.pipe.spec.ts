import { AvrGrowthPipe } from './avr-growth.pipe';

describe('AvrGrowthPipe', () => {
  it('create an instance', () => {
    const pipe = new AvrGrowthPipe();
    expect(pipe).toBeTruthy();
  });
});
